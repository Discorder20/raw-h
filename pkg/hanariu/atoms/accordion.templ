package hanariu

type AccordionBosons struct {
	Collapse bool
}

templ Accordion(props *AccordionBosons) {
	if props.Collapse {
		<div class="accordion-143">
			{ children... }
		</div>
	} else {
		<div
			class="accordion-262"
			data-x-data="{selected: 1}"
		>
			{ children... }
		</div>
	}
}
